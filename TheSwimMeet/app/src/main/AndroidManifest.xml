<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.theswimmeet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewSQLiteData"></activity>
        <activity android:name=".collegeSwimTeamFilter" />
        <activity android:name=".news" />
        <activity android:name=".timeStandards" />
        <activity android:name=".myPoints" />
        <activity android:name=".myGoals" />
        <activity android:name=".editAccount" />
        <activity android:name=".myProfile" />
        <activity android:name=".createAccount" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>